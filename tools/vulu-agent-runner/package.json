{
  "name": "vulu-agent-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "tsx src/server.ts serve",
    "fallback-sweep": "tsx src/server.ts fallback-sweep",
    "github-signal": "tsx src/server.ts github-signal",
    "print-prompt": "tsx src/server.ts print-prompt"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  }
}
